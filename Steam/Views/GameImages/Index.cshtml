@model IEnumerable<Steam.Models.GameImage>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Game.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UrlString)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Game.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UrlString)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GameImageId }) |
            @Html.ActionLink("Details", "Details", new { id=item.GameImageId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GameImageId })
        </td>
    </tr>
}

</table>
